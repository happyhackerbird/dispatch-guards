# dispatch-guards — ledger

On-disk ledger for this repo: one entry per line, append-only,
chronological. Facts carry their basis, decisions carry their why
(and the rejected alternative where it is not obvious), open
questions stay listed until they close. Absence of an entry never
reads as settled.

**Consumer:** any session working in this repo — read the tail before
re-deriving something that may already be settled, and append here
rather than leaving a rationale only in a commit message. Boundaries:
work items go to `BACKLOG.md`, standing rules to `CLAUDE.md`,
maintenance journals to `dev-notes/`.

Retrofitted 2026-08-06 from `git log`; only decisions whose rationale
had no home outside a commit message were seeded, so the record
before that date is deliberately sparse rather than complete.

## Entries

- **2026-08-05 (13a21f4, first instance 2026-08-01 40f22ec) — a hook
  file ships mode 100755, and the `--test` battery cannot catch it
  when it does not.** `hooks.json` execs hooks directly via `/bin/sh`,
  so a 100644 hook dies with "Permission denied" and, being fail-open,
  gates nothing — silently. Twice: `push-claim-reminder` (0.1.12,
  reminder absent) and `dispatch-skill-gate` + `discovery-volume-`
  `reminder` (0.3.2, no dispatch ever gated; found by the operator
  reading per-call error spam, hours later). Basis for the blind spot:
  the bite-tests passed throughout both, because they invoke via
  `sys.executable`, which never consults the x-bit — so a green battery
  is not evidence a hook can launch. Mechanised at both altitudes in
  the SIBLING dotfiles repo, off this repo's read path: pre-commit
  `nonexec_hook_commands` and `bootstrap/doctor.py`
  `plugin_hook_exec_verdict` / `check_plugin_hook_files` (both verified
  present 2026-08-06).

- **2026-08-06 (7ee9b35) — the 0.5.2 skill rules were applied here
  verbatim, not designed here.** Both-divergence-directions in §1, the
  reviewer-tier cap in §4, and the register consult keying on the
  TARGET repo's procedure class were settled in a prior session and
  persisted in `dotfiles docs/directives/session-b-handoff.md` — a
  sibling repo, which a fresh context in THIS repo does not load.
  Re-opening any of them means reading that file; re-deriving them
  from the skill text will not recover the grounding (e.g. the §1
  base-check split exists because the observed live case was
  HEAD-ahead, not HEAD-behind).

- **2026-08-06 (this commit) — the "no `LEDGER.md`" deviation is
  reversed; `LEDGER.md` joins the role files.** The deviation carried
  its own revisit condition — "revisit if multi-session work here
  starts re-deriving settled ground" — and that condition has fired:
  multi-session corpus work is running in this repo now. Rejected
  alternative (the deviation's original reasoning): letting commit
  messages plus `dev-notes/` carry decisions. That holds only while
  the horizon is short — a commit message is found by someone who
  already suspects what to look for, which is exactly what a
  re-deriving session does not have.

- **2026-08-06 — RULED (was OPEN; see the closing clause): `EXECUTION_TAIL_BG` (brief-reminder.py
  :414) and the EXECUTION tail in `plugin/skills/dispatch/references/`
  `forms.md:108` have really diverged, and the commissioned drift
  detector was HALTED rather than shipped.** Measured under the
  specified normalization: 5 normalized lines vs 27; three substantive
  content divergences (the ≤3000-chars sentence rewritten around
  split-parts-not-a-file; the backgrounded-check/AWAITED sentence
  absent from the literal; the never-amend sentence absent), plus
  forms.md's `<channel line>` placeholder against the literal's
  concrete background channel line. Two decisions the detector cannot
  be built without, neither of them the executor's to make: whether
  comparison is per-line (as specced — unsatisfiable, since the two
  sides are wrapped by different rules) or whitespace-insensitive over
  the whole block (what `brief-reminder._norm` does, and why); and how
  the placeholder line maps. Both files sit outside the halted
  dispatch's write boundary, so the drift itself is also unrepaired.
  RULED 2026-08-06, same day, by the dispatching session: the
  whole-block whitespace-insensitive comparison, with the documented
  background channel line substituted for `<channel line>` — but in
  repair-first order, because the baseline is currently red for real
  reasons: sync the literal to the shipped tail (keeping the
  deliberate independent-copy design), THEN build the detector, THEN
  red-proof by mutation against a now-green baseline, and give
  `tools/check-doc-drift.py`'s `main()` the could-not-verify third
  outcome it lacks. Carrier of the full ruling and its grounding:
  `dotfiles claude/BACKLOG.md`, commit `6889d12` — a sibling repo a
  fresh context here does not load, which is why the ruling is
  restated on this entry rather than pointed at. Severity correction
  belonging with it: `EXECUTION_TAIL_BG` sits inside
  `if __name__ == "__main__"` (brief-reminder.py:371), so it is a TEST
  FIXTURE and was never emitted to an agent — the consequence is that
  this guard's bite-tests pass against a tail shape the form no longer
  produces, which is the frozen-fixture class, not misinstruction.

- 2026-08-08 SHARED-INDEX SHARPEN + STORED-BRIEF STALENESS (build-first,
  operator GO "lest fix dispatch from here now"). Two amendments, both
  in place, both from one parallel dispatch on a shared working copy.
  (1) `git add <paths>` does not isolate a shared copy — the INDEX is
  shared, so a co-writer staging between an agent's `git status` and
  its commit rides out under that agent's message whatever paths it
  added. Observed: a five-file commit under a three-file message,
  carrying the co-writer's in-flight work. `git commit -- <paths>`
  isolates because it ignores the index for everything else. The
  existing clause already NAMED the hazard ("a clean targeted git add
  absorbs them") and prescribed the very form that cannot prevent it —
  a rule that diagnosed correctly and then recommended the disease.
  Generalised in the same edit: in a shared repo any state read is
  stale by the next command, so check and act belong in ONE command or
  in a form that cannot act on the wrong object — pathspec for a
  commit, an explicit hash for a reset, never `HEAD~1` (observed: a
  `reset --soft HEAD~1` aimed at the mixed commit un-committed a
  co-writer's newer one instead). Audited across all four homes that
  prescribed `git add`: dispatch §1 + its brief skeleton, the §2
  EXECUTION tail in references/forms.md (the one pasted into every
  brief, so the highest-leverage), and executor §6 "the box".
  (2) CLAUDE.md's brief-family clause: a STORED brief is a label over
  a body written at a past date, and its grade records
  decision-completeness AS OF then. Grounding: of six backlog items
  audited, two were stale — one whose diagnosis was refuted by a
  different investigation ("rotation rewrites the live file" had cause
  and effect inverted) and one whose evidence proved a badly-written
  probe rather than a missing feature. The refuted one had SURVIVED a
  fresh-context vet with its wrong hypothesis intact, because the vet
  checked the entry's reasoning and not the world. Both capability
  patches; fire-rate review judges them.

- 2026-08-08 — SHARPEN §1 (build-first, operator GO), widening the
  commissioned-instrument bullet upward: what a brief must fix is the
  instrument's SEMANTICS, of which absence-mapping was only the first
  instance. Second instance, the one that grounded the widening: a
  COMPARISON names its GRAIN. Grounding — a brief of mine specced a
  LINE-SEQUENCE comparison between brief-reminder.py's
  EXECUTION_TAIL_BG literal and the forms.md EXECUTION tail; measured
  under that exact normalization it is 5 literal lines against 27
  forms.md lines, a WRAP difference rather than a content one, so no
  state of the two files could make the check green. The dispatcher
  specced an unprovable check — the class the corpus already names,
  authored into a brief by the person who knows the class. Amendment
  over addition: the bullet's trigger abstracted upward rather than
  growing an "…or X" list; forms.md §3b's Exactness clause is
  source-labelled from the new text as the same rule inside the
  enumeration form, not re-stated. Executor side audited, no edit —
  its gaps-surface-never-bridge conduct rule already covers an
  instrument brief arriving with its grain unstated. Verify block all
  four green (bench 40/40, bites, devbook form, doc-drift, wrap).
  Durability: CAPABILITY PATCH; fire-rate review judges it.
2026-08-08 NAME-LANE STAGING BYPASS RATIFIED (operator decision, decision round item 3; parked item leaves by this commit): the mandatory-name lane keeps deny-from-day-one — check()'s exit-2 path structurally precedes guard_mode(), the repair is compose-time and mechanical (add the `<model>-` name), which is the false-fire profile staging exists to protect against; rework of check() through fire()/guard_mode() DECLINED (generality no current lane needs). Declared exception recorded beside the staging rule in CLAUDE.md; future lanes earn the same exemption only by their own operator decision.
2026-08-08 BENCH-RED REMEDIATION COMPLETE + ESCALATION ORDERING DECIDED (operator decision round items 2+4; both halves of the 0.6.x-verify-RED item and the deny_text item leave the backlog by these refs). (a) d2fdfdb (sonnet dispatch): guards.jsonl:31 silent→block, the retired-title-lane case. (b) ac8341b (sonnet dispatch): escalation_deny() now precedes check() in main() — DECISION: an escalating subagent's FIRST bounce teaches return-the-question, never "add a name" toward a call that must not succeed; two-bounce path rejected; ordering pinned by a second inspect.getsource assert (escalation_deny < error = check) plus a no-leak corpus case (NAMED subagent ask-tier → still deny). (c) 3369ccf (sonnet dispatch): missing_channel deny_text(payload) — named dispatches get the followable repair (background channel line), the sync-flag advice stays for unnamed only; red-first via battery-against-old-signature. Verified at the desk at HEAD: both --test batteries green, replay-bench 41/41, catch 16/16, false fires 0 (first fully green bench since 0.6.x). 998c9b7 bumps 0.7.1 ONCE for the batch (unbumped_plugins compares release-state, so the per-author repair commits follow bump-first — the sequencing both executors correctly halted on; brief lesson: parallel same-plugin writers need the version-bump gate pre-named, bump owned by the dispatcher).
2026-08-08 SIX BRIEF-FORM CLAUSES MINTED (operator GO on the §1 batch at the fable desk; all six READY entries leave by c282a04, bump 6a49a47 → 0.8.0): sweep surfacing (per-hit disposition accounting + instrument-positive member), criteria-as-outcomes-then-sites (graduated from dev-notes — second firing proved the shelf was the problem, not the wording), commit-plan-vs-guards sequencing (bump position derived from the guard's OWN comparison basis — checklist finding: the first draft stated one guard's release-state mechanism as universal, scoped before landing), pre-authorized repair classes (declared-exemption shape applied to briefs), per-line Background provenance grades ("from <source>, unverified" — the phantom-citation class), §3b data-file key-set quote (jq from the file, never prose — the all-null misread class). Verified: bench 41/41 unchanged, doc-drift clean, wrap 0 violations, skill-lint clean except the dispositioned forms.md:29 dead-cite (accepted 0.7.1, observation ea63c47 in skill-craft). JOURNAL line: dotfiles corpus repo, same session.
2026-08-08 WORKTREE LIFECYCLE ENGAGED — SKILL AMENDED + REPORTING DOCTOR SHIPPED (operator standing GO; the 859f965 incident is the grounding): worktree SKILL.md "Cleanup and litter" gains two rules the incident proves and the section lacked — `git worktree list` is SHARED state with no ownership dimension (nothing in `.git` records who created a worktree, so a sweep over the whole list sweeps other sessions' work), and the plain-remove refusal on a dirty worktree IS the safety mechanism, `--force` being the entire difference between cleanup and unrecoverable loss. Basis: reproduced independently at the desk in a scratch fixture — plain remove refuses with "contains modified or untracked files, use --force to delete it"; the incident's loop verbatim destroys all three arms including the dirty one. DECISION, direct conflict created by 859f965 resolved in the text: the pre-existing branch bullet's "dispose of each by merge, cherry-pick, or an explicit drop" at worktree-cleanup time contradicted the new "do not delete branches as part of any worktree cleanup" — the bullet keeps its SURFACING duty (enumerate branches created for worktrees at integration) and loses its disposal-at-cleanup clause, because branch survival is precisely what kept the incident's committed work recoverable; branch retirement is its own decision on its own trigger. Description gains "cleaning up or removing worktrees" as a trigger — the new rules' firing moment is a sweep, and no trigger covered it. Not corpus (worktree = portable git mechanics, skill-craft alone).
2026-08-08 DOCTOR DESIGN — OWNERSHIP IS DECLARED, NEVER INFERRED (dispatcher decision at brief time, filling a gap the BACKLOG entry left open): the entry fixed the doctor's verdicts and verifier but not how it knows which worktrees are the caller's, while "what marks ownership durably" is the entry's own PARKED question. Resolved by `--owned <path>` (repeatable) with everything undeclared reported UNKNOWN, never FOREIGN — the tool cannot know. Rejected: any predicate over path shape, name prefix, directory component, branch name, or commit trailer — that is the naming-convention predicate the entry rejects up front, and left to the executor it would have been re-created inside the very tool built to prevent it. This does not prejudge the parked question: a future durable mark simply auto-populates the flag, and UNKNOWN-by-default is what makes could-not-verify honest rather than a fallback.
2026-08-08 PATHSPEC-COMMIT RULE WAS FALSE FOR FILE CREATION — SHARPENED ACROSS ALL THREE HOMES (grounding: the dispatch's own blocked commit; triage = rule fired and was WRONG, so a sharpen in its existing seat, not a new entry): `git commit -- <paths>` cannot commit a wholly-new untracked file — "error: pathspec '<p>' did not match any file(s) known to git" — so the corpus rule "never `git add` then `git commit`" was unsatisfiable for any dispatch whose deliverable is a new file. Fix minted at dispatch/SKILL.md (write boundaries), dispatch/references/forms.md (the pasted EXECUTION tail — the home that actually reaches executors), executor/SKILL.md §6 (the box, with a source label to dispatch §1): `git add -N <path>` first, intent-to-add. Basis, probed at the desk rather than taken from the agent's report: index entry is the EMPTY blob e69de29 (zero content staged), the commit still carries the file's FULL body, and a co-writer's separately staged file and staged tracked-file modification both remain staged and uncommitted through it. The rule's reason (shared index) is untouched — this is a carve-out, not a softening.
2026-08-08 SUBAGENT ESCALATION HANDLED CORRECTLY, RECORDED AS THE POSITIVE CASE: the sonnet dispatch met a plugin-payload version-bump pre-commit gate that its write boundary gave it no way to satisfy (plugin.json outside its lane), and the gate's own message advertised `--no-verify` as the audited bypass. It bumped nothing, bypassed nothing, left the co-writer's file untouched, and returned the question with three named paths. Dispatcher took path (c) — the bump and release are the dispatcher's act. Candidate lesson booked from it: a brief granting a narrow write boundary in a plugin-payload repo should say up front whether version-bump gates are expected to fire and who resolves them.

2026-08-10 QUOTE-PARSE-FEHLER HOEREN AUF, COMMITS IN PUSHES ZU VERWANDELN — und die Regel, die den Bau haette glatt machen sollen, feuerte zum DRITTEN Mal nicht. Welle (opus-Dispatch, decision-complete Brief `dotfiles docs/directives/dispatch-guards-wave-2026-08-10.md`): `12bd40a` member A — `is_push_command`s `except ValueError`-Lane retokenisiert jetzt per Whitespace und laeuft durch dasselbe argv-Positions-Praedikat (`_push_in_tokens`) wie der geparste Pfad, statt auf einen Substring-Matcher abzusteigen; die drei Substring-Regexe sind tot und geloescht. `b362279` members B/C/D — drei §1-Klauseln (shared checkout hat kein privates Rot; das Write-Set-unveraendert-Praedikat samt Reichweiten-Grenze "eine Import-Liste ist nicht die Abhaengigkeits-Liste"; REPOINT-Schritte bestaetigen den Knopf). `5341918` member E — dieselbe Defekt-Klasse in `amend-gate.py`, gefunden vom Executor beim Nachbarlesen, samt des Kommentars, der meinen geaenderten Kontrakt zitierte und dadurch falsch geworden war. `a06b175` — die EXECUTION-Tail-Fixture holt forms.md ein (Drift seit `994d977`, Dispatcher-Arbeit). ZWEI DEFEKTE, DIE NIEMAND PFLANZTE: das alte Fallback brach auch die `git remote set-url --push`-Ausnahme, sobald ein Apostroph im Befehl stand (Config-Write als Push verweigert) — als Fixture behalten statt wegjustiert; und der deployte 0.10.13-Waechter verweigerte den Commit, der ihn repariert, mit dem Autor als Subjekt. DISPATCHER-VERIFIKATION mit eigenem Instrument: acht Shapes durch `is_push_command` (ROW1 jetzt False, ROW4/ROW5/CTRL weiter True, beide Ausnahmen halten), vier durch `is_amend_command`, die drei eingefuegten Bloecke per Substring-Identitaet gegen den Brief mit Known-Negative-Kontrolle (mutierter Block -> False), Bench 44/44 mit den neuen Faellen ATTRIBUIERT (amend-gate 5->7, subagent-push-gate 10->11), Katzen-Rate 17/17, Fehlfeuer 0. DER EIGENTLICHE BEFUND IST DIE REGEL, NICHT DER WAECHTER: `SKILL.md:284` (commit-plan-vs-guards) wurde am 2026-08-08 aus GENAU diesem Versagen gemintet — LEDGER:146 und :151 buchen zwei Vorfaelle desselben Tages, der zweite ausdruecklich als candidate lesson "ein Brief mit enger Write-Boundary in einem Plugin-Payload-Repo soll vorab sagen, ob Version-Bump-Gates feuern und wer sie aufloest". Am 2026-08-10 schrieb ein Dispatcher mit geladenem Skill den Brief zum dritten Mal ohne Commit-Plan, und der Executor lief in dieselbe Wand. Drei Vorfaelle, eine Regel, gemintet und inert: die Konsequenz ist kein vierter Satz daneben, sondern ein PRAESENZ-Slot im §1-Skelett (`## Commit plan`), dessen Fehlen im Artefakt sichtbar ist — dieselbe Medizin wie Route-Line, Gauge-Line und `**Red executed.**`. Als POINTER im dotfiles-BACKLOG gebucht; JOURNAL-Zeile fuer die Korpus-Aenderung im Korpus-Repo, dieselbe Session.

2026-08-10 THE COMMIT-PLAN SLOT EXISTS NOW, AND IT HAS A MECHANISM UNDER IT (opus dispatch closing the POINTER entry booked one entry above; bump `a11954c` → 0.10.15 first, because this repo's own payload-version gate compares against the RELEASE state). §1's brief skeleton gains `## Commit plan` between `## Write boundaries` and the §2 tail block — the presence slot whose absence is visible in the artifact, "none" a valid filling and silence not. DISPATCHER DECISION on a gap the entry left open, surfaced before any build and answered before it landed: the entry's Design sentence changed only the skeleton while its Verifier demanded a red from the brief-reminder fixture pair, and a pair no predicate can tell apart grades nothing — so the slot got a PREDICATE, `missing_commit_plan`, scoped exactly like `missing_sections` (Agent tool, execution-tail anchor, brief = prompt plus referenced brief files). Shape decided AGAINST widening the live `missing_sections` deny lane, which would have shipped a deny by assertion and denied every existing execution brief lacking the slot: this repo's CLAUDE.md:32-33 says a new lane ships default-warn and earns deny through the fire-rate review, so the lane routes through `fire(..., default_mode="warn")` — the first `fire()` lane in a file whose four older lanes call `deny()` directly, which means `guard_modes` key `brief-reminder` reaches this lane alone. RED-FIRST ARRANGEMENT, stated because the arrangement is the basis: the expectations live in a scratch file runnable against ANY hooks directory, and the OLD side was a copy of the WHOLE hooks dir at `a11954c` — never a revert of the working tree, which would have removed the new expectations along with it and passed old-against-old vacuously. Unmutated baseline first: the old module's own `--test` green. Then the new expectations against that old module: `AssertionError: brief-reminder exposes no missing_commit_plan`. After the build the same file reports True/False on the pair. END-TO-END at the guard's real altitude, same payload through stdin: the old binary emits only the reminder line, the new one emits the staged WARN, both exit 0 — and a brief whose slot is filled `none` keeps the reminder and draws no warn. KNOWN CONSEQUENCE, documented at the call site rather than discovered later: `fire()` exits in every mode, so a warn replaces the generic reminder line for that one brief. `README.md:33` says four deny lanes plus one staged warn lane. NOT DONE, surfaced not bridged: `tools/corpus/guards.jsonl` carries zero brief-reminder cases (8 guards, 44 cases, none this hook), so the bench does not reach this lane and its net is the `--test` battery the repo's verify block already names for the guards the bench misses.

2026-08-10 THE SHARED-CHECKOUT CLAUSE REACHES THE SIDE THAT ACTUALLY PERFORMS IT (same opus dispatch, second POINTER entry). `b362279` landed "a shared checkout has no private red" in the dispatch skill alone, but both obligations it states are the EXECUTING side's — declaring a deliberately-left red in the report, and reporting a blocked commit or push instead of reaching for `--no-verify` — and an executor never reads the dispatch skill. CLAUDE-maintenance's corpus-wide audit rule already required this: the two skills are the sending and receiving sides of one discipline, so a rule amended in either is audited across both. Amended in place inside executor §1 rule 6 (the box) rather than minted as a new rule — the box already carries the shared-working-copy premise for the shared INDEX, so the shared-red fact co-locates with it, and both homes now carry a source label to dispatch §1. Basis, instrument shown live before the zero was believed: `grep -nE "no-verify|foreign red|REPO-WIDE|tree red|transient red|shared checkout|private red"` returned 5 hits in `dispatch/SKILL.md` and 0 in `executor/SKILL.md` at `f80fd4f`, with the known-positive control `grep -c "report" executor/SKILL.md` → 13 proving the pattern reaches the file; after the amendment the executor side returns 6. Wrap clean at 69 columns; bench 44/44, catch 17/17, false fires 0, doc-drift clean, every hook `--test` green.

2026-08-15 THE §2 CHANNEL RULE NOW KEYS ON `name`, AND THE PROBE INVERTED THE FIX THAT WAS BRIEFED FOR IT (opus desk, closing the PARKED 2026-08-08 item by running its named missing evidence; 0.10.19). FACT, schema-level: the Agent tool takes no `run_in_background` parameter on harness 2.1.232 — `additionalProperties: false`, key absent — so the axis the 2026-08-08 entry, the parked item and the incoming handoff brief were all built on does not exist. FACT, measured: the lane is selected by `name`. A NAMED dispatch (generic or pinned type alike) returns "Spawned successfully … via mailbox", fires no completion notification and does not appear in ListAgents' subagent listing; an UNNAMED one returns "Async agent launched" with an output file, and its completion task-notification carried the agent's final text VERBATIM with `tool_uses: 0` — the agent sent nothing and the text arrived anyway. The separating cell was pinned+NAMED: earlier evidence compared generic+named against pinned+unnamed and moved two variables at once, which is why two prior observations left the question open. DECISION, against the handoff brief's own prescription: forms.md §2 does NOT default pinned types to the background line — on the unnamed lane "your final text reaches no one" is FALSE and the sync line is TRUE, so that fix would have been wrong in the other direction. The brief's diagnosis had rotted while its fix still read plausibly. ROOT DEFECT was the predicate, not the prose: `is_background()` read `run_in_background is not False`, constant TRUE once the key vanished, so every dispatch classified background and `tail_mode_mismatch` denied the unnamed lane's correct line — the guard manufacturing the defect it exists to prevent. `mailbox_lane()` replaces it. TWO DEFECTS FOUND WHILE BUILDING, each its own commit: `deny()` never called `fire_log()` (six hard-deny lanes invisible to the fire-rate review; zero deny entries all-time for brief-reminder against 673 for one fire()-routed lane), and a live FALSE FIRE where `missing_sections` denied a verifier brief whose only offence was CITING forms.md — the guard reads referenced .md files and forms.md carries both tails — repaired by a declared exemption (`_tail_kind()` reads the prompt first), never a softened predicate. INSTRUMENT NOTE worth re-reading before writing the next bite-test: the first test for that exemption PASSED against the unfixed code because its fixture used a RELATIVE path with no `cwd` in the payload, so no file was read and no anchor was present — it verified nothing, and only the red-first run exposed it; it now uses an absolute path and asserts the anchor is present before testing. brief-reminder had ZERO replay-corpus cases before this batch (the gap LEDGER 2026-08-10 booked as NOT DONE); six added, two red against the old hook, one reproducing the original bounce.
2026-08-17 ERSTER WARTUNGS-PASS ÜBER dev-notes/dispatch-OBSERVATIONS.md, 0.11.0 (Peer-Handoff vom fable-Desk; schließt BACKLOG READY 2026-08-14). Alle lebenden Einträge haben einen Ausgang: 20 Amendierungen in dispatch §1/§4, forms.md §2 + beide Tails, executor Regel 4/6, worktree-Cleanup-Klausel; 8 waren bereits angewandt (Beleg je Eintrag); 2 verworfen (fire-log-Hypothese am Resolver widerlegt — XDG_DATA_HOME wie im README; writer-claims-HEAD-Variante und Lane-STATE-Register, letzteres weil „Lane geschlossen" zur Hook-Zeit nicht komputierbar ist); 3 als RESIDUUM stehen geblieben, weil ihre Zielstelle das guard-checker-bau-Devbook in dotfiles ist, das diese Arbeitskopie nicht schreibt. Der geplante CUTOFF für die 15 Vor-Form-Einträge war nicht nötig — einzeln gegradet, fünf davon längst angewandt. Struktur: `## Abgeflossen` stand in der Dateimitte, während neue Einträge hinten anwuchsen (fünf lebende Einträge lasen sich als abgeflossen) — Abschnitt ans Datei-Ende, lebende unter `## Offen`. Zwei Funde des Passes selbst: (1) worktree/SKILL.md wies weiter an, Reader-Worktrees „at the booking" zu entfernen — die vierte Heimat, die die 0.10.25-Amendierung („drei Heimaten") nicht erreichte, belegt per `git log -L` und `git show --stat`; (2) die Umstellung des READ-ONLY-Tails riss brief-reminders READ-ONLY-Anker, gefangen vom Korpus-PREMISE-PIN (erwartet context, beobachtet deny) — das war der Rot-zuerst-Beweis, Reparatur als Marker-FAMILIE (alt+neu), damit Briefe im Flug nicht fehlfeuern.
2026-08-17 RELEASE 0.11.1 — Wartungs-Pass + Frisch-Kontext-Runde, Pin 094486a (Version und gitCommitSha geprüft). REVIEW-GRAD, ehrlich benannt: Enumeration durch eine sonnet-Lane über 36 Einträge (32 LANDED, 0 NOT-LANDED, 1 DISCARD-OK, 0 DISCARD-BAD) plus Desk-Grading, DANACH eine opus-Frisch-Kontext-Runde über den finalen Stand — der erste opus-Versuch starb am Wochenlimit, bevor der Operator das Konto wechselte; die Runde lief also vollständig, aber erst im zweiten Anlauf. Sie fand sechs Befunde, alle angewandt: (1) der Struktur-Nachtrag im Träger beschrieb noch den ERSTEN Fix, nachdem der zweite ihn umgedreht hatte — ein Etikett, das seinen Körper überlebt, in der Datei, deren Pass genau davon handelt; (2) der neue Sync-Lane-Absatz ist vom eigenen Model-Gate verboten (gemessen: generischer Typ ohne `name` → exit 2, mit Name → exit 0) — das Gate behält seine Lane, forms.md nennt jetzt die Kollision und routet sie an den Operator, und der Gate-Docstring, der den ersetzten Satz zitierte, trägt den Vorbehalt (der Audit war skill→skill gelaufen und hatte den Docstring ausgelassen, den CLAUDE.md als kanonisch erklärt); (3–5) fünf grün-aber-kaputte Zustände von check_observations_tail, alle konstruiert und gemessen: erste statt letzte Abgeflossen-Überschrift, ein führendes Leerzeichen überspringt die ganze Datei, `###`-Ebene unsichtbar, Marker-Test las den letzten Absatz statt „nichts folgt dem Marker", und der CouldNotVerify-Arm war unerreichbar (getrace't) — der Check hatte gar keine dritte Antwort; Probe zweimal gelaufen, also graduiert zu `--test` mit 11 Armen; (6) drei Einträge ohne echten Ausgang, darunter der von diesem Pass selbst gemintete, der sich in seinen Slots als abgeflossen auswies und unter `## Offen` stand — das Spiegelbild des behobenen Fehlers. Eigene Prozess-Lehren, aufgezeichnet weil sie Arbeit kosteten: ein `git stash` in einer als read-only gerahmten Probe leerte den Arbeitsbaum um die halbfertige Reparatur (per Stash zurückgeholt, nichts verloren; der gewollte Vergleich kam ohnehin aus `git show`), und ein fusioniertes commit+push wurde vom eigenen push-claim-reminder korrekt verweigert — der Deny blockierte den GANZEN Befehl, also war auch der LEDGER-Append nicht passiert, was der Zustands-Check zeigte statt der Erinnerung.
2026-08-17 RETIREMENT-PASS BACKLOG + drei gebaute READY-Einträge, 0.11.2. Quote war fällig (Banner: gebucht ~4 vs geschlossen ~1); der Pass las die Einträge gegen die Welt, die dieser Tag geändert hat, statt sie umzusortieren. Gebaut statt neu gebucht (die Buchung hätte so lange gedauert wie der Fix, und die Zielstelle war ohnehin offen): (a) untracked-outputs deckt jetzt JEDE Datei unter geteiltem Scratch-Root, nicht nur werkzeug-DEFAULTS — der vom Agenten GEWÄHLTE Name ist der, der durchrutscht, weil eine Regel über Defaults sich für ihn als nicht zuständig liest; (b) §1 trägt den Compose-Zeit-Zwilling des executor-seitigen Outward-facing-Verbots — ein Verifier, der innerhalb des Write-Sets nicht grün werden kann, GEWÄHRT den Schritt in Worten oder das Kriterium wird ersetzt; (c) die Section-Map nennt §§2/3/3b als Überschriften von references/forms.md, damit jede „§2"-Zitation auflöst. Zwei Funde des Passes selbst, beide Träger-Hygiene: der Eintrag zu (b) verortete das Verbot in §1, während ein Governed-Set-Scan es im executor-Skill findet — stale premise genau der Klasse, die dieser Tag in §1 amendiert hat (Design unberührt, Zitat nicht); und ein READY-Eintrag trug ein abweichendes Bullet-Format, war damit für jede grep-basierte Zählung unsichtbar — die Zählung sagte 5 READY, es waren 6. Re-gradiert statt gelöscht: der worktree-LIFECYCLE-Eintrag, dessen Vorschläge 1–3 im Doctor gebaut sind; geparkt bleibt allein Vorschlag 4 (Retirement-Trigger) mit unveränderter genannter fehlender Evidenz. Bestand danach: 14 offen (3 READY, 10 PARKED, 1 RE-GRADED), 3 Abgänge per Commit-Ref.

2026-08-18 ZWEI OBSERVATIONS-EINTRÄGE UND EIN PEER-NACHTRAG IN SKILL-TEXT ÜBERFÜHRT, 0.11.3 (Peer-Auftrag vom pbs-office-Backlog-Desk, Betreiber-GO dort erteilt; Commits 657531d Bump + 78ea1e5 Payload). HERKUNFT EHRLICH: der Auftrag kam als Peer-Testimonium über einen Betreiber-Befehl, nicht vom Betreiber direkt — er BRAUCHTE ihn aber nicht, weil das Session-Start-Banner den Wartungs-Pass für dispatch-OBSERVATIONS ohnehin schuldete (gebucht ~7 vs abgeflossen ~0). Die Arbeit ist durch die eigene Quote gedeckt, nicht durch die Weiterleitung. SKOPUS-ENTSCHEIDUNG, gegen den vollen Pass: nur die zwei benannten Einträge plus den Nachtrag, NICHT die übrigen 8 offenen dispatch-Einträge. Basis ist die Serien-Tendenz aus dem Träger selbst, nicht Bequemlichkeit — 0.11.0 fuhr 20 Amendierungen in einem Batch, und die 0.11.1-Frisch-Kontext-Runde fand danach sechs Defekte, von denen mehrere der Pass SELBST gemintet hatte (der Struktur-Nachtrag, der seinen eigenen Körper überlebte; der vom Pass erzeugte Eintrag, der sich als abgeflossen auswies). Kleiner Batch kauft Attribution; die Quote bleibt über das Banner sichtbar, das ist der Träger, keine Erinnerung. DREI REGELN: (1) Skip-Disposition in vier Heimaten, als POSITIVE Pflicht statt als Aufmerksamkeits-Aufforderung — der Eintrag warnt ausdrücklich davor, weil der Vorfall nur durch zufällig rote Nachbarn ans Licht kam. (2) Worktree-NACHBARSCHAFT plus die Vorfrage an der Rung-2-Entscheidung. (3) §4-Horizont um die Nachfolge-Hälfte erweitert. ABWEICHUNG VOM VORFORMULIERTEN TEXT, bewusst und einzige: der Eintrag verlangte die Skip-Regel „wörtlich in beide Tail-Blöcke"; der READ-ONLY-Tail führt gar keine Slot-Liste, also steht sie dort als Klausel über die BASIS des Verdikts — Verifier-Lanes sind genau die, die Checks laufen lassen, die Lücke wäre sonst offen geblieben. EIGENER FUND DES PASSES, nicht aus einem Eintrag: worktree/SKILL.md:93 schrieb `--git-common-dir` als Auflösungsweg für den Hook-Pfad vor — genau das Kommando, dessen cwd-Relativität Vorfall (b) misst; das Skill trug die Falle in seiner eigenen Anweisung. Am laufenden git gemessen, diskriminierendes Paar aus der Quelle: Haupt-Checkout `.git` bzw. `../../.git` je nach cwd, aus /home/g resolved zu /home/g/.git (exists False), im Worktree bereits absolut, `--path-format=absolute` beide Male richtig; Probe-Worktree entfernt, Config-Hash vorher/nachher identisch. BASIS-GRAD des Nachtrags, getrennt gehalten wie der Absender ihn trennte: Mechanismus gemessen (Peer, n=2), SCHADEN vorhergesagt und nirgends beobachtet, von diesem Desk NICHT reproduziert — die vorgeschriebene Handlung (Horizont aufschreiben) ist unter beiden Wahrheiten billig, deshalb gebaut statt geparkt. MECHANISIERUNG DER SKIP-REGEL ABGELEHNT UND GEBUCHT (BACKLOG PARKED): die Skip-ZAHL ist computierbar, die ANWESENHEIT einer Disposition nicht ohne Fehlfeuer — ein Report, der „4 skipped (unrelated, pre-existing)" schreibt, disponiert im selben Satz. Genannte fehlende Evidenz: echte Report-Körper aus dem Fire-Log. TRÄGER-NEBENBEFUND, vom doc-drift-Check gefangen statt von Hand: der Append-Marker in dispatch-OBSERVATIONS.md stand bei 3c70504 nicht mehr am Datei-Ende — die zwei Peer-Commits hatten unter ihm angehängt; zurückgesetzt. 0.11.3-Grab-bag (BACKLOG READY 2026-08-17) mitgenommen, weil sein Done-Kriterium wörtlich „landet mit dem nächsten Version-Bump" lautet und dieser Pass der Bump ist.

2026-08-18 FRISCH-KONTEXT-RUNDE ÜBER DEN 0.11.3-PASS: DREI DEFEKTE IN EIGENEM TEXT, EINER DAVON SELBSTBEZÜGLICH (opus, Verifier-Dispatch mit Artefakt+Frage allein, Bericht in 7 Teilen; Fixes `fda5b1a`). Die Runde war die Paarung des Skip-Gauges (stilles Versagen) und hat sich bezahlt: sieben Amendierungen geprüft, fünf HOLD wie geschrieben, drei echte Defekte plus zwei Nits und EIN Fund, den weder der Eintrag noch dieses Desk hatte. DER SELBSTBEZÜGLICHE FUND, der wertvollste: die Nachfolger-Hälfte der Horizont-Regel stand da, wo der Nachfolger sie nicht trifft — das dispatch-Skill lädt per Hook-Gate zur DISPATCH-Zeit, und eine Session, die einen Handoff erbt und nichts dispatcht, lädt es nie. Eine Regel über das Aufschreiben von Pflichten hatte ihre eigene Einlösungs-Anweisung nicht in den Träger gelegt. Behoben: die geschriebene Pflicht trägt die Disposition mit, nicht nur Agent/Brief/Base-Commit. NEUER FUND ohne Vorlage: die Options-REIHENFOLGE trägt — `git rev-parse --git-common-dir --path-format=absolute` ignoriert das Flag still und liefert wieder relativ; an diesem Desk nachgemessen (git 2.55.0, Flag voran absolut / Flag hinten `.git`), steht jetzt als Warnung im Skill, weil ein "Aufräumen" des Flags den Bug wortlos zurückholt. EIGENER STEMPEL-DEFEKT, und die Lehre sitzt in der Schicht, nicht im Satz: die SendMessage-Erreichbarkeit trug "(measured — …)" ohne Staleness-Stempel, obwohl sie eine HARNESS-BINDUNG ist und die beiden anderen Bindungen desselben Skills gestempelt sind — eine davon fünfzehn Zeilen darüber. Die Provenienz war in LEDGER und Commit-Message korrekt gegradet (Peer, n=2, hier nicht reproduziert); GELECKT hat die In-File-Darstellung, die mehr behauptete als die Provenienz-Schicht trägt. Das ist die Stelle, die zählt: die Regel wird im Skill gelesen, nicht im Ledger. DANGLING REFERENT gestrichen statt umformuliert: "die Ausnahme entschuldigt den Schluss, nicht den Dispatch" kam aus einem repo-lokalen Schluss-Gate des Absenders; verallgemeinert verneinte der Satz eine Ausnahme, die weder das Skill noch accretion.md:191 kennt. Ein Klausel-Import aus fremder Repo-Mechanik in portablen Korpus wäre die falsche Richtung. EIN FUND ABGELEHNT MIT GRUND (nicht alles, was ein Reviewer nennt, ist ein Defekt): der EXECUTION-Tail bekommt kein Quellen-Label, weil er mit "the §2 form here" eröffnet — er IST die Form, nicht ihre Entlehnung; das Label wäre zirkulär und hätte die Hook-Fixture für null Gewinn nachgezogen. Der READ-ONLY-Tail bekam es, weil er Slot (b) in eine Lane ohne Slots BORGT. METHODEN-NOTIZ, weil sie den Unterschied machte: der Reviewer hat den doc-drift-Check ROT-BEWIESEN statt seinem Grün zu trauen — HEAD per `git archive` in den eigenen Scratchpad gespiegelt, Baseline zuerst grün gemessen, dann NUR die Fixture zurückgedreht und den Rot-Ausschlag an normalisiertem Zeichen 222 gezeigt; und er hat die Reichweite dieses Beweises ehrlich begrenzt (nur diese eine der sieben Drift-Prüfungen ist als diskriminierend belegt). Das ist die Anordnung, die dieses Desk beim eigenen Fixture-Fix NICHT gefahren hat.

2026-08-20 DOC-DRIFT-LANE `observations tail` FEUERTE AUF LEGITIME ARBEIT — PRÄDIKAT VERENGT, FEHLFEUER-KONTROLLE IN DIE EIGENEN ARME GELEGT (n=2 für die Klasse). Vorfall: ein Peer-Append (2026-08-20-Eintrag, Überschriftenform `## <Datum> — <Titel>`) ließ `check-doc-drift.py` rot laufen, obwohl der Marker genau dieses Anhängen am Datei-Ende ANWEIST. Ursache im Prädikat: `headings_all` zählte `ln.startswith("#")`, also auch EINTRAGS-Überschriften — und die dominante Eintragsform dieses Trägers IST `## <Datum> — …`. N=2, aus dem Ledger selbst: der 0.11.3-Pass (2026-08-18) hat denselben Ausschlag als echten Fund gebucht ("der Append-Marker stand bei 3c70504 nicht mehr am Datei-Ende … zurückgesetzt") und ihn per HAND repariert, indem er den Marker nach unten schob — die Umgehungs-Form, an der ein Fehlfeuer als Befund durchgeht. WIDERSPRUCH IM SPEC, der das ermöglichte: der Docstring behauptete zwei Sätze, die zusammen unerfüllbar sind — "wer anhängt, schreibt ans Datei-Ende, und keine Konvention schlägt das" UND "die letzte Zeile ist der Append-Marker". Für Überschriften-Einträge kann nur einer gelten. ENTSCHIEDEN nach dem motivierenden Vorfall (welche Lesart feuert dort?): der Vorfall war ein Eintrag, der IN `## Abgeflossen` landete — das fängt die Abschnitts-Lesart weiterhin (`live[-1] < drained[-1]`, Arm "sections swapped", plus neuer Arm "drained section opened after the marker"). Also verengt auf STRUKTURELLE Abschnitte (`drained_re`/`live_re`), und die Marker-ist-letzte-Zeile-Behauptung aus dem Docstring gestrichen statt umformuliert. ROT-ZUERST, Anordnung benannt: Baseline zuerst grün gemessen (alle Arme bestanden), dann der Fehlfeuer-Arm ("heading-form entry appended after the marker — legitimate") gegen das ALTE Prädikat gefahren → genau dieser eine Arm rot, während die Positiv-Kontrolle im selben Lauf grün blieb — das Paar diskriminiert, eine immer-rote Prüfung ist ausgeschlossen. Nach der Verengung: `--test` alle Arme grün, echter Träger grün, replay-bench 57/57 unverändert. REPARATUR-FORM nach Repo-Regel: die legitime Fassung liegt als Daten IM Netz des Checkers (deklarierte Ausnahme, die der Check selbst verifiziert), nicht als aufgeweichtes Prädikat ohne Kontrolle. Betroffene Datei mitcommittet: dev-notes/dispatch-OBSERVATIONS.md trug zwei uncommittete Fremd-Einträge (MERGE 2026-08-19 vom cache-fix-Desk, 2026-08-20 vom Fable-Desk) — Shared-File-Serialisierung, auf Bitte des Fable-Desks mit diesem Commit übernommen, inhaltlich unverändert.

2026-08-20 KEEP-VERDIKT AUFGENOMMEN, DAMIT DER GERUCH NICHT ERNEUT UNTERSUCHT WIRD (corpus-harmony F16, Fremdbefund): der §-Nummernraum über die DREI Dateien (dispatch/SKILL.md führt §§1,4,5,6; references/forms.md führt §§2,3,3b; references/routing.md die Tier-Evidenz) ist gegen den Pin 75a52e5 als KORREKT verifiziert worden — Reviewer (frisches Opus) und Fable-Desk graden ihn übereinstimmend als bewussten Trade-off, keine Aktion. Aufgenommen, weil ein wiederkehrender Verdacht ohne aktenkundiges Verdikt jedes Mal neu untersucht wird; die Sektionskarte im SKILL.md-Kopf ist die Stelle, die ihn auflöst. HERKUNFT EHRLICH: Befund und Grad stammen vom Fable-Desk-Review, nicht von diesem Desk; hier NICHT nachgeprüft — der Eintrag ist ein Zeugnis-Grad-Record, keine eigene Messung. Volltext mit Beid-Seiten-Zitaten: dotfiles claude/records/corpus-harmony-review-2026-08-20.jsonl (zur Relais-Zeit noch ungepusht).

2026-08-20 DREI-LANE-BATCH GEBAUT, FRISCH-KONTEXT-RUNDE FAND ZWEI BLOCKER, EINE LANE ZURÜCKGENOMMEN (Betreiber-GO als Peer-Testimonium vom Fable-Desk relayed; die Arbeit war ohnehin durch die eigene READY-Menge gedeckt). GEBAUT UND GEHALTEN: (a) Chain-Note im Deny-Renderer `_dispatch_common._deny_payload` — jede verkettete Bash-Verweigerung sagt jetzt, dass NICHTS lief (1abe70a); (b) §6-Register-Zeilen im brief-reminder-Advisory, an genau dem Moment, an dem die Tier-Wahl fällt (4e7f61a); (c) Register-Pin in replay-bench plus dessen eigener Isolations-Selbsttest als Verify-Schritt 1a (f2e4dcf). ZURÜCKGENOMMEN: handoff-report-gate (0b666fc → Revert 286484a) — zwei gemessene Prämissen falsch, beide für JEDE Fixture der eigenen Suite unsichtbar: (1) das Ending-Turn-Fenster („Events nach der letzten user-Rolle") schließt den geprüften Akt aus, weil ein tool_result SELBST ein user-Rollen-Event ist (in einem echten Transkript 99 von 106; alle 7 SendMessage-Aufrufe von einem gefolgt) — am echten Hook gemessen: Fixture-Form + gesendet → still, ECHTE Harness-Form + gesendet → FEUERT, nie gesendet → FEUERT, der Wächter unterscheidet Pflichterfüllung nicht von Pflichtverletzung; (2) das Marker-Tor feuert auf gewöhnliches Korpus-LESEN — über alle 303 echten Transkripte dieser Maschine 15 Feuer, keines ein Handoff, 14 davon fangen den Platzhalter `<name|operator-terminal` aus der §4-Klausel des eigenen Skills, teils in fremden Projekten. Die Behauptung „false-fire profile near zero" stand in Docstring UND README und war durch Messung widerlegt; Arm (iii), die deklarierte Fehlfeuer-Kontrolle, modelliert nur „kein Marker" und kann das strukturell nicht sehen. REVERT STATT PATCH, begründet: „Handoff empfangen" von „Text mit dem Marker gelesen" zu trennen ist eine Design-Frage; ein Prädikat-Tweak hätte die Messung gegen sich behalten. Der Eintrag geht PARKED zurück und trägt jetzt beide Messungen plus die 303-Transkript-Menge als stehendes Instrument. FEUERLOG-KONTAMINATION, gefunden von derselben Runde, real und außerhalb des Repos gelandet: die Bites schrieben in `~/.local/share/claude/dispatch-guards-fires.jsonl`, weil weder die neuen Bites noch der Doctor-Sweep FIRELOG bzw. XDG_DATA_HOME pinnen — 33 Test-Datensätze, davon 24 auf einer taufrischen Lane, die damit 24 All-Time-Feuer auswies, kein einziges aus echter Arbeit. Genau das Instrument, das die warn→deny-Beförderung liest. Bereinigt mit Backup (`.bak-2026-08-20-precleanup`) und einem DISKRIMINIERENDEN Prädikat statt einer Schätzung: session_id null/"s1" hatte in 47 historischen push-claim-Datensätzen 0 Vorkommen und heute 9; Sicherheitsprüfung „kein als Test klassifizierter Datensatz trägt eine echte session_id" lief vor dem Schreiben. 51 echte Datensätze erhalten. EIGENE INSTRUMENTENFEHLER DIESES DESKS, gezählt weil das Verhältnis die Lehre ist: FÜNF kaputte Sonden gegen NULL kaputte Lanes — Fixture-Schema geraten statt gelesen (`processes` statt `prozesse`), Payload benutzt, die die eigenen Deny-Lanes vorher abfangen, escaped JSON mit Klartext-Filter gelesen, `--include=*.md` unquotiert von zsh gefressen (0 Treffer als Abwesenheit lesbar), und im Isolations-Selbsttest zweimal am Ziel vorbei gemessen (abgeschnittenes `detail` statt rohem stdout; Pro-Arm-Tempdir, das die Arme aus Harness-Gründen unterscheidbar machte). Jede Sonde gab zurück, was ein echter Defekt zurückgibt. Was den Lauf brach: eine bekannt-konforme Payload AUS DEM KORPUS ziehen statt eine zu erfinden. LEHRE, an den Skill-Text zu geben: vor dem Ausliefern eines Parsers für ein Format, das der Brief nur in PROSA beschreibt, die reale Instanz lesen, wenn eine existiert — die Register-Lane hat genau so ihren eigenen Schema-Fehler gefunden (ihre erste Fassung hätte `_hinweis`/`schema_version` als Klassen gerendert), und ihre zwei vorgeschriebenen Bites wären unter der falschen Fassung grün geblieben, weil sie deren eigene falsche Fixture benutzt hätten.

2026-08-23 DIE REPORT-PROVENIENZ-REGEL ERREICHT DIE AUSFÜHRENDE SEITE, NICHT NUR DEN DISPATCHER (opus-Lane `opus-report-provenance`, C1; Bump `f6b7d94` → 0.11.5 zuerst, weil das Payload-Gate gegen den RELEASE-Stand vergleicht). Sieben Commits, alle von der Lane, hier gebucht weil das pre-push-Gate Subagent-Commits ohne Record-Eintrag zu Recht blockiert: `f6b7d94` Bump · `554e36c` §2 Prosa · `dbdd81a` OBSERVATIONS Vorfall · `a4e9385` OBSERVATIONS writer-reservation-gate Fehlfeuer n=4 · `f7af084` §4 Empfangs-Hälfte · `d2f04e8` beide Tails + Fixture · `b20a6bf` Träger-Abfluss plus KLASSE. DIE KLAUSEL, wortgleich in beiden Tails: jede Behauptung über etwas AUSSERHALB der eigenen Arbeit nennt den Read, der sie geöffnet hat, oder trägt "inferred, unverified"; eine Empfehlung, die auf einer ungeöffneten Behauptung ruht, trägt den Grad mit. DER EIGENTLICHE FUND, und er ist die Klasse: die Regel lag seit `554e36c` korrekt in §2 — gelesen wird §2 aber vom DISPATCHER, der den Report gradet, während die Pflicht den AGENTEN trifft, der ihn schreibt. Wo ein Korpus eine KOMPRESSIONSSCHICHT hat (ein gepasteter Tail, ein Template, eine generierte Zusammenfassung — jedes Artefakt, das den Korpus für ein zweites Publikum neu rendert), ist die Landung im Korpus die halbe Änderung, und die andere Hälfte hat kein Tell: die Datei liest sich richtig, jeder Check ist grün, und der Verpflichtete bekommt die Regel nie. n=2 mit Beleg, beide in derselben Datei: dieser Vorfall und §2s eigener Record, dass Channel- und Payload-Regel die ausführenden Agenten "nur als Gate-Denials, zu doppelten Kompositionskosten" erreichten, bis sie getailt wurden. ROT-ZUERST, Anordnung benannt weil das Grün allein nichts beweist (die Fixture war vor der Änderung ebenfalls grün, als KEINE Seite die Klausel trug): Baseline unmutiert exit 0 "no drift"; dann NUR die Fixture zurückgedreht, forms.md geändert gelassen → exit 1, erste Divergenz an normalisiertem Zeichen 929, also an der Position der Klausel selbst und nicht an einem beiläufigen Offset; wiederhergestellt exit 0. Die Arme unterscheiden sich, der Check diskriminiert auf DIESE Klausel. VORFORMULIERTER FIX FÜR DIE KLASSE, mit seiner eigenen Grenze im Text statt versteckt: eine doc-drift-Lane, die für jede §2-Klausel mit Agenten-Pflicht ein Tail-Gegenstück behauptet, ABER auf einem deklarierten Marker-Token verschlüsselt — "Klausel, die eine Agenten-Pflicht nennt" ist über freie Prosa nicht maschinell entscheidbar, und ungemarkert wäre die Lane ein Keyword-Checker, also genau die Fehlfeuer-Form, die dieses Repo bereits benennt und ablehnt. NICHT VERIFIZIERT, von der Lane selbst benannt: der READ-ONLY-Tail hat KEINE Fixture — doc-drift pinnt allein den EXECUTION-Tail, seine Kopie der Klausel ist durch Lesen belegt und durch nichts Mechanisches; und keine der drei Heimaten ist je AUSGEÜBT worden, es wurde nichts mit dem neuen Tail dispatcht, der Drift-Beweis zeigt Übereinstimmung der KOPIEN und nie, dass die Klausel ändert, was ein Agent schreibt.
2026-08-27 WAVE-4 ITEM A: THE WAVE-3 OBSERVATIONS APPLIED TO THE SKILL — SIX COMMITS, BOOKED HERE BECAUSE THE PRE-PUSH GATE RIGHTLY BLOCKS A MARKED SUBAGENT COMMIT WITH NO RECORD ENTRY (same remedy as the 2026-08-23 entry; the blocked sha was `007e4de`). Written in English: the operator's standing decision is that these artifacts are English, and the sibling sweep for the observation carriers is already booked (BACKLOG READY 2026-08-27) — a new German entry would add to that debt. LANE `opus-a-skill-amendments` (opus, peer desk dotfiles-2f dispatching, fable judgment desk grading): `a3111b2` eight of nine sites into SKILL.md §1/§4 and forms.md §2 · `691d4b2` three 2026-08-27 observation entries DRAINED by moving them into `## Abgeflossen`, the mailbox entry deliberately left live because one of its four clauses could not land. LANE `opus-a2-tail-and-background`: `007e4de` the mailbox clause into the EXECUTION tail's HEAD with `EXECUTION_TAIL_BG` in the SAME commit · `e582e9a` the Background-line rule into §1's brief skeleton. DISPATCHER: `8b12c43` three findings booked. JUDGMENT DESK: `569c72d` carrier entries. WHY THE TAIL AND ITS FIXTURE RIDE ONE COMMIT, measured twice independently: `check_execution_tail_fixture` compares forms.md's tail against the hook's literal on normalised text, so ANY tail edit is red until the literal moves with it — lane A was briefed with a write set excluding `plugin/hooks/` and could not land its item at all; it proved this on a scratch copy whose OWN doc-drift run it made green FIRST, so the red belonged to the clause and not to the arrangement. Booked READY as a Verify-prose sentence. THE OFFSET IS A FUNCTION OF PLACEMENT, NOT OF THE DEFECT: lane A's red reported first divergence at normalised char 1780 with the clause at tail position, lane A2's at char 0 with it at the head — so an offset quoted from a prior lane's red is provenance, never an expected value to match against. SCOPE CORRECTION THAT PREVENTED A DUPLICATE HOME: the ninth site's source bullet holds two sentences and its SECOND was already landed as site (c) from entry `f123de0`; only the first was owed. Found by lane A2 checking `a3111b2` before writing, not by the dispatcher. THE SOURCE SET WAS DERIVED FROM THE CARRIER, not from the handoff's count of "six entries dated 2026-08-27" — only four carry that date, and one named site had no carrier entry at all; its written source is the wave-4 handoff's standing orders. The missing entry is the judgment desk's to write and is named as owed rather than invented. DISPATCHER VERIFICATION before this push, re-run at the artifact rather than booked from the reports: replay-bench 57 cases/57 match, catch 30/30, false fires 0; `--test` isolation pinned; per-guard bites 15 run/0 failed/0 SKIPPED; check_devbook_form and worktree_doctor pass; check-doc-drift 7/7 [ok] including the EXECUTION tail fixture; manifests parse. A2's four-word deviation `(source: §2, the delivery binding)` ACCEPTED: its placement scan found the tail is a deliberate SECOND home for that rule, and the maintenance doctrine requires a source label at each restatement — applying an existing form to a fact the brief did not know is not executor-designed rule text. DISPATCHER'S OWN INSTRUMENT FAILURE, recorded because it nearly produced a false defect against correct work: a line-based grep for A2's landed sentence returned ZERO because the phrase spans the 69-column wrap; re-run whitespace-normalised with a known-positive control it was present. Third instance the same day by the same desk, all after the rule was loaded — the sharpening (the bite lands when one BUILDS A COMPARISON over wrapped text, where the false result arrives shaped like a verdict rather than like a zero) is booked as a corpus amendment, dotfiles df-134.

2026-08-27 WAVE-4 ITEM D2: THE THREE DEV-NOTES OBSERVATION CARRIERS TRANSLATED TO ENGLISH — `437159c`, LANE `sonnet-d2-carriers` (sonnet; peer desk dotfiles-2f dispatching, grading and integrating; judgment desk claude-code-cache-fix-d8). Scope: `dev-notes/dispatch-OBSERVATIONS.md`, `worktree-OBSERVATIONS.md`, `OBSERVATIONS-FORM.md`, under the operator's standing decision (2026-08-27) that every persisted artifact is English. DISPATCHER VERIFICATION, re-run at the artifact rather than booked from the lane's report: commit shas and ISO dates extracted independently from both sides and set-compared — identical in all three files (75/159, 4/14, 0/1); entry counts identical at every heading depth (`## ` 60/5/3, `### ` 28/5/0), so the commit's net −63 lines is prose compression and not a lost entry; `check-doc-drift.py` 7/7 `[ok]` including "observations tail", `--test` all passed; `german-scan.py` re-run by the desk reproduces the lane's figures exactly (10/8/1 hits). THE DEVIATION IS CORRECT AND WAS PROVEN, NOT ARGUED: the German section anchors `## Offen`/`## Abgeflossen` and the append marker's FIRST LINE STAY, because `check_observations_tail` keys on those exact literals (check-doc-drift.py:332-334, 382-383, plus ~8 `--test` fixtures) and the lane's write boundary excluded that file. CORRECTED IN PLACE 2026-08-27, judgment desk's precision (dotfiles `da00ecf`), verified here before accepting it: of the marker comment's four lines, ONLY the first carries a parsed literal — lines 2-4 in each of the two carriers, six lines in total, are ordinary German prose that nothing parses and that the wider sweep may translate freely. The first wording said "the append marker STAY", which reads across all four lines while the predicate establishes one: an assurance wider than its predicate, in a record whose whole subject is a load-bearing literal. Measured per line with the three parsing patterns applied individually (2737 `_APPEND_MARKER`; 2738-2740 none). The lane established it by executing the pair — full translation red on an isolated copy, anchors-only revert green, confirmed both ways — and the desk's independent repo-wide dependents grep (positive control non-zero) returned the same set. Agreement on a shared coordinate, not one instrument's green. PUBLIC-REPO PUSH: authorized from this repo's own `## Carve-outs` heading (operator decision 2026-08-27, recorded in CLAUDE.md), never from a peer's testimony that such an authorization exists; disclosure checked before pushing — the commit introduces NO new home-path or credential-shaped strings (same 3 distinct `/home/…` paths, same counts, already public in the German original; instrument control non-zero). PRE-PUSH GATE FIRED CORRECTLY and this entry is its remedy: the lane's commit is marked as a subagent commit and sat in no record carrier, so the push was blocked until booked here — the gate was satisfied by writing the record, never by `PUSH_UNBOOKED_SUBAGENT_OK=1`. DISPATCHER'S OWN MISREAD, recorded because it briefly produced a false defect against correct work: reading the last six headings of a 3314-line carrier, the desk concluded `## Offen` was absent and that the translation had broken the marker's reference; the section is at line 2341 and nothing was broken — a partial view read as the whole body, second instance by this desk today. LESSON BOOKED, not left in chat: `dev-notes/dispatch-OBSERVATIONS.md` gains an entry for the class the lane surfaced — a brief that changes TEXT the target repo's own tooling parses must name the dependents search as an explicit step, because the corpus's dependents rule does not fire when the thing changed reads as prose for humans. That entry deliberately does not quote the append marker verbatim: the check matches it by substring, so quoting it would plant a second marker in the carrier it governs.
2026-08-28 WAVE-5 ITEM S: HEAD ITEMS (ii)(iii)(v)(vi) LAND IN THE TWO SKILLS — THREE COMMITS, BOOKED HERE BECAUSE THE PRE-PUSH GATE RIGHTLY BLOCKS A MARKED SUBAGENT COMMIT WITH NO RECORD ENTRY (same remedy as the 2026-08-23 and 2026-08-27 entries; the blocked sha was `360b864`). Lane `opus-dispatch-wave5-head` (opus; peer desk `dotfiles-a7` dispatching, grading and integrating; judgment desk `claude-code-cache-fix-14`). Commits: `360b864` manifest 0.11.6 → 0.11.7 as its own FIRST commit — the bump-first plan, which this wave measured rather than inherited (three arms in a scratch clone: payload without bump REFUSED, payload+bump in one commit ACCEPTED, and with the bump committed-but-UNPUSHED a second payload commit ACCEPTED, because the guard compares against origin and not against the parent; wave 4's one-commit rule was forced by that repo having NO origin, and its premise died when the remote appeared) · `43f2a99` the two skills, +68 lines, additions only · `1952bfb` the two drained observation entries. PLACEMENT, the one design decision: (ii) is a lane-conduct duty and lands in BOTH carriers — the receiver half in `executor` §1 rule 1, revised IN PLACE rather than added, because a new rule would renumber 2-7 and silently falsify the `conduct rule 5` cross-reference in dotfiles CLAUDE.md; the sender half in `dispatch` §1 as a brief part. Placing it only in `dispatch` would have minted a receiver duty into the sender's carrier, against §1's own "the conduct layer travels by skill, never restated per brief" — the reading the judgment desk gave when the peer desk surfaced it as the item's one open decision. DEVIATION ACCEPTED: the (iii)/(v) record citation went into the commit message, not the skill text, because `CLAUDE-maintenance.md` §Provenance lets an incident survive in-file only DE-PARTICULARIZED and "wave-4 record :244-247" is a dated named artifact carrying a position index — it also keeps a private-repo path out of a public skill. THE LANE'S CRITIQUE PASS CAUGHT THE BRIEF'S OWN DEFECT, which is head item (ii) working on its first use: the dispatcher's verifier list transcribed five of this repo's SIX §Verify commands and the dropped one was the 69-column wrap check, whose diff scope is literally `plugin/skills/dispatch` + `plugin/skills/executor` — the only check that grades the FORM of that lane's own edits. Every listed check stays green over an 80-column line. Verified at the artifact by the peer desk: replay-bench 57 cases / 57 match / catch 30-of-30 / 0 false fires, `--test` isolation pinned, doc-drift 7 ok and no drift, manifests parse. The wrap check was run BOTH ways and the pair is the finding: as written it diffs against `HEAD`, so with a clean tree it grades 0 bytes and prints `wrap: clean` vacuously; re-pointed at the base it graded the 68 landed lines and returned clean for real. OPEN: the operator's installed plugin lags at 0.11.6 until `claude plugin update dispatch-guards@dispatch-guards-marketplace` — reported as owed, run by nobody here; and every rule in this commit is INERT in every running session until that update lands, so the commit is not the rule taking effect.
2026-09-12: decision (operator GO, dotfiles session): §1 "What rides ONE lane" write-side rewritten — disjoint small items DEFAULT to one lane below the crossover (per-item commits + red-first arrangements keep attribution; diagnosis never bundles); "token spend is identical either way" re-scoped to parallel-vs-queued; one review round per wave unless a dependent lane consumes a verdict earlier. Drains the 2026-08-15 observation's write-side half (its 2026-08-17 application was read-only-half only). Corpus complement: dotfiles 5bcd338 (calibration pull-seam, routing lane-count sizing).
2026-09-12: fix + decision (judgment desk, executed bases): plugin/hooks/_dispatch_common.py exec bit REMOVED (100755→100644, disk and index) — the pre-commit x-bit/shebang guard's direction-B fire was CORRECT per its own docstring (updated this day by a drain-wave lane: population deliberately all-0755-under-payload, motivated by the 2026-08-17 notify-suite live hang from copying this very file's mode), so the fix is the file's mode, never the guard's population; the earlier BACKLOG entry's two branches were both wrong — hooks.json does not exec it (4c's grep, 16 entries, absent) AND the guard does not over-include (mode is the defect). 0.11.10. The migration desk's blocked commits unblock on this landing.
decision: regrade: fill goal, write-set, done-criterion and evidence, or drop → moot (closed by dg-31)
decision: this is a skill edit: CLAUDE.md Discipline requires a skill-craft invocation (pre-edit hook enforced) and a skill-craft:release-plugin release with an operator /reload-plugins handoff, which is a release act on the machinery desk's cadence, not a migration act → moot (closed by dg-32)
2026-09-12: fix (judgment desk, executed red both sides): push-claim-reminder's fused-push deny missed NEWLINE-separated fusion — the default multi-line Bash shape; _CMD_POS gains (?<!\\)\n\s* (escaped newline stays a continuation). Found by dotfiles-4c after the daneel foreign-commit publication (d4b6ea3); both incident commands are now byte-exact fixtures, red on the old pattern (measured at two desks), green-side pair moved fixtures only. 0.11.12.
2026-09-12: fix (judgment desk, red per side on byte-exact transcript strings): fused-push deny's SECOND population gap same day — wrapper words (timeout N, command) and git global flags (-C/-c) defeated both patterns, so three inline fused pushes (drain desk C/F/E) passed 0.11.9 AND 0.11.12. The 0.11.12 explanation ('newline class, companion side') was REFUTED by the transcript — inferred from the instrument's silence to the world's shape, and the validating probes shared their author's bare-word spelling. All three strings are battery fixtures now. 0.11.13.
fact: codex-routing.md rewritten from the statiker pilot (operator global-usage GO 2026-09-12, stated first-hand in statiker-58): certified roles (read/attack/implementation ready per model, reviewer EXCLUDED on a failed replay, desk eval-open) + measured harness bindings (exit-0-on-API-failure, read reach unpinned, .git write-denied so dispatcher commits, sandbox-depressed suites, quota-kill empties); evidence home statiker dev-notes/codex-pilot-2026-09-12.md + READINESS.json; entries candidate-in-operation outside statiker; stale gpt-5.5 framing removed; JOURNAL line owed in dotfiles (its copy is another session's) — pre-formulated text sent to dotfiles-85 this date
2026-09-12: mint (judgment desk, operator GO in wan2gp-71; incident measured this date): dispatch SKILL.md §1 write-boundaries gains the DISPATCHER as a co-writer of the shared copy. Incident: a dispatched lane's pathspec commit absorbed the desk's uncommitted rewrite of the very file the brief assigned; the desk's `git status` then read clean and the work was indistinguishable from work it never did, found only by reading the lane's commit body, unpicked with an explicit-hash reset. Classified loaded-but-inert, not a gap: the disjointness clause was loaded and correct, but every co-writer enumeration in §1 names third parties (peer session, operator, scheduled job, agent, human), so the desk composing the boundary reads itself as its AUTHOR rather than a party inside it -- a scope-binding defect, fixed by widening the existing clause (amendment case 3), not by a new bullet. Placement basis: scan over plugin/skills/ for text placing the dispatcher inside the boundary returned ZERO hits (`command grep -rniE "dispatcher('s)? own (uncommitted|edits|hunks|work in)|desk('s)? own uncommitted"`); the two enumerations are SKILL.md:198 and :283. Executor side audited and unchanged -- its pathspec commit is correct as instructed, and the absorption is the documented residual ("for a shared FILE no safe form exists"), so the duty is the dispatcher's. Medium question answered PROSE: a hook at dispatch time would need the brief's write set, which is prose, and a dirty-tree predicate over-fires on every normal repo -- mechanism bar unmet, desk as backstop. Review-checklist passes run one axis at a time (no-op, reference binding, salience, register, abstraction, enforcement/medium, ladder); two findings, both repaired before landing: "this rule" unbound over a multi-rule bullet, and "every enumeration above" false for the one at :283. 0.11.15.
2026-09-13: fact (drain desk, statiker-fd): a5a9584 (0.11.19) adds the site-overlay declaration line to references/routing.md's Site overlay heading — the overlay now names ethos's overlay-disjointness.py as its restates-nothing checker. Subagent lane commit, graded at the dispatcher against its four-part closing report (item df-198, dotfiles carrier; ethos 4034124 is the check itself). Wiring into any gate lane is deliberately NOT part of this commit.
2026-09-15: fact (judgment desk dotfiles-89, guard-wave G1): 019e856 + 567a940 (0.11.22) land the brief gate's commit-plan lane at DENY — promotion per the staged-lane rule on its honest fire record (47 warn fires, 39 post-repair of the one recorded false-fire class 372dcc7, >=6 desk-confirmed true, no truth-value field so never a measured zero) — and the new staged devbook-pin WARN lane, LAST in main() because fire() exits in every mode (an early warn lane would disable the four denies behind it; the brief's original ordering clause transferred the named-diagnostic rule outside its mechanism and the lane refused it). Realizes dotfiles df-238; subagent lane opus-brief-gate-g1, five-part report graded and re-verified at this desk (bench 59/59, 0 false fires; brief-reminder --test green; both e2e arms in the hook's own battery). Residue booked: dg-43 (bench register fixture), dg-44 (bench warn kind), dotfiles df-250 (devbook ordering-boundary amendment). README brief-reminder row re-rendered same push (20d41df) — the doc-drift checker keys on filenames and cannot see an in-file lane change.
decision: codex adopted globally for its certified roles (operator, 2026-09-15, via the dotfiles judgment desk) — mechanical-read lanes default to luna, astra runs beside opus at attack rounds, terra takes single-artifact brief-covered patches; verify and review stay Claude (operator-excluded: Claude beat codex on verify yield at equal honesty). Two-window limit fallback (short rolling + weekly, refusal = backpressure, weekly = codex out of lineup until reset) specified in codex-routing.md; detection binding unstamped until first observed refusal. Rejected alternative: keeping the entries statiker-scoped candidates — adoption IS the grading mechanism, and the fire-rate review backstops it.
2026-09-15: fact (executing desk dispatch-guards-c2, guard-rewrite arc wave 0): both probes GREEN. A PreToolUse hook's `updatedInput` prompt edit REACHES the subagent's effective prompt (arm a7 injected line quoted back by the agent; arm a8 control clean, token absent), and a rewrite applies with NO `permissionDecision` field (b2) while the permission flow still runs on the rewritten call (b3 denied under a purpose-built ask gate). Arm b4: a hook-forced `allow` did not override a settings `ask` rule, with b5 as the positive control proving the harness reads `permissionDecision` from this hook with this schema -- so b4 is a finding, not a malformed-output artefact; its scope is a permission RULE only and it grounds no general no-dialog-suppression claim. Probe A's instrument was strengthened over the directive's sentinel: the agent quotes its received instructions verbatim, which separates DELIVERY from OBEDIENCE (in a7 the agent quoted the injected line rather than obeying it). Full record with every command and observed string: docs/audits/wave0-probe-record-2026-09-15.md. Directive's STOP-and-escalate condition did not fire; waves 1-2 authorized by the judgment desk.
2026-09-15: decision (judgment desk dispatch-guards-bc, guard-rewrite arc): brief-reminder's missing_tail lane becomes MODE-AWARE when item 2 converts it to a rewrite -- mechanically decidable class rewrites under both `deny` and `warn` (the repair is the action, not a punishment grade), the AMBIGUOUS class takes the mode-aware exit, `off` means no rewrite and no deny. Why: deny() does not consult guard_modes at all (the hook's own main() comment; confirmed live in wave 0 when modes set to off did not stop the lane), so today that lane cannot be demoted by a site. SCOPE: only missing_tail this arc; the other three deny lanes stay as they are. Rejected alternative: making deny() mode-aware globally in the same change -- booked as dg-48 (PARKED, decision blocker) so the global question carries its own fire-rate case instead of riding this arc.
2026-09-15: decision (operator, relayed via judgment desk dispatch-guards-bc; landed by the executing desk): CLAUDE.md Discipline gains the THREE VERBS bullet -- deny, warn, and rewrite via `hookSpecificOutput.updatedInput` -- so every new guard lane and lane amendment names its verb and why, the rewrite is required to live inside the gate it satisfies (matching hooks all see the original call), a verb conversion carries the lane's existing staging record forward rather than re-staging it, and deny stays right where the deny IS the point. Cites the hooks doc (fetched 2026-09-15) and the wave 0 probe record including b4's negative with its scope limit. Booked by the carrier's own cost test as a DO-IT-NOW rather than an item: one file, one hunk, session live -- `item add` refused the booking and said so. Downstream: dotfiles df-253 was evidence-blocked on this bullet existing (its predicate greps this file for updatedInput; now 1 hit).
decision: should the description-derived slug strip a legacy model title prefix, against the directive's literal spec → YES, narrowly (judgment desk, 2026-09-15): strip only when the leading word EQUALS the call's validated model; a different model word stays. Basis: the enshrining bite is same-parentage. Design and rejected alternative in dg-49
2026-09-15: fact (executing desk dispatch-guards-c2, guard-rewrite arc): the authorized repair of dg-40's broken blocker predicate was NOT executed, deliberately. The judgment desk authorized rephrasing it from a broken evidence-predicate to a DECISION blocker on the reading that the item 'always in fact needed a decision', and invited a stop if that reading proved contestable. It is contestable: the entry's done-criterion names the reservation-gate fire tally as its basis, and that tally is on disk and computable now (751 rows, all warn, 2026-08-10 to 2026-09-15). So the item is not waiting on a decision at all -- it waits on a MEASUREMENT whose raw half is already available and whose discriminating half (absorptions missed and silent) a fire log cannot supply by construction, since it records fires and never non-fires. Rephrasing to a decision blocker would have recorded the wrong kind of wait and buried a live instrument gap. Evidence recorded in dg-40's own evidence slot; the blocker stands broken and flagged, and the entry needs an instrument decision rather than a form repair. dg-34's repair is unaffected and still owed at the wave boundary.
decision: should the legacy-model-prefix strip extend to the name source as well as the description → YES (judgment desk, 2026-09-15, wave 2b): strip from either source when the leading word equals the call validated model; a different model word stays, per 2(b). Ruled fix-now rather than book-past because the release seam is here and the doubling is a defect in the lane being shipped
2026-09-15: fact (executing desk dispatch-guards-c2): guard-rewrite arc CLOSES. Build items landed and pushed -- dg-45 model-gate name lane deny->rewrite (df4a850), dg-46 brief-gate missing_tail mode-aware with the one-direction decidable class (a0533fc), dg-49 + dg-50 the legacy-model-prefix strip on description then name (f37862b, 186edbb). Every lane verified at this desk rather than booked from its report: full verify block re-run here each wave, plus mutation testing against a green control (widened strip RED, forced-decidable RED, truncated tail RED). ARC ACCOUNTING against the directive's BUILD declaration -- opened 8 (dg-45..dg-52), closed 4 (dg-45, dg-46, dg-49, dg-50): the declaration that a BUILD run closes at least as many items as it opens is NOT MET, net +4. Every one of the four still open is work this arc could not close at its own size: dg-47 needs its own operator GO (the directive said so), dg-48 is a decision the arc deliberately declined to fold in, dg-51 lands in another repo's boundary, dg-52 is the maintenance drain the desk ruled out of a BUILD run. Recorded as a shortfall rather than argued away -- the declaration exists to make this number a verdict, not a tally read afterwards.
2026-09-18: decision (operator, scope-agent session "is dispatch guard not over engineering for a small subagent"; GO "yes"): dispatch tiers land (0.11.27, `_dispatch_common.dispatch_tier`, policy key `light_tiers`, default on). read = LIGHT_READ_TYPES; small-write = one `Writes:` line, <=2 paths, prompt <=2000 chars, no heavy word (push/publish/deploy/commit/release/merge/DB/artifact/served/email/delete). Light tiers skip dispatch-skill-gate and brief-reminder's form lanes; model gate, mailbox channel lane and tail_mode_mismatch still apply (tail_mode_mismatch moved ahead of missing_tail so it runs before the light exit). Why: fire log 2026-09-12..18 = 395 fires / 53 sessions, median 6 per session; skill gate repeated in 14 of 27 sessions; incident = one-file draft swap bounced 4x (skill, tail, sections, model). Rejected: removing lanes outright (the push/publish/served class is where the form earns its cost). Not a new deny lane, so the default-warn staging rule does not apply; each light pass logs `light-read`/`light-small-write` so the relief's use is countable. Red-proof: 4 of 9 new corpus cases mismatch against 0.11.26 hooks, 73/73 on 0.11.27.
2026-09-18: open: SKILL.md §1 does not yet mention the tiers (skill edit needs the skill-craft pass); discovery today is the deny-text hint (`light_tier_hint`) and README.
